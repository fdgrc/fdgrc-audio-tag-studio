# AudioTags V1.6.2.2 — Transcription Default Hotfix

- On-device Whisper is now the default on desktop and mobile whenever supported.
- Desktop helper is restored automatically only when a pairing token already exists.
- Engine labels now make the no-pairing path explicit.
- PWA cache bumped so deployed clients receive the corrected UI.
